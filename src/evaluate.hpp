#include <string>

#include "types.hpp"
#include "tokenize.hpp"

#ifndef _EVALUATE_HPP
#define _EVALUATE_HPP

/*
 Let's start with a simplistic case: no parenthesis, no unary operator
*/
ObjectPtr evaluate(std::string expr)
{
}

#endif
